# Minimal dbt project for testing dbt-guard (PII governance / lineage).
# Compilable with: dbt compile (from this folder, with DBT_PROFILES_DIR=.).
name: dbt_guard_example
version: "1.0.0"
config-version: 2
profile: default

model-paths: ["models"]
