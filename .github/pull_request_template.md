## What and why

<!-- What does this change, and what problem does it solve? Link an issue if there is one. -->

## Checklist

- [ ] `gofmt -l .` prints nothing
- [ ] `go vet ./...` passes
- [ ] `go test ./...` passes
- [ ] `./scripts/test-e2e.sh` passes
- [ ] Added/updated tests for the behavior change (or explained why not)
- [ ] Updated relevant docs (`README.md`, `docs/`, `examples/`) if behavior or CLI usage changed
